export interface NavbarPros{
    scrolled?:boolean;
    scrollPosition?:number;
}


export interface isHoveringProp{
  isHovering?:boolean;
  isShopping?:boolean;
  isAboutHovering?:boolean;
  scrollPosition:number;
}


export interface openProps{
  open:boolean;
  scrollPosition:number;
}